I came across tensorflow and I kinda grew interest on knowing how it works. First tensorflow datatype is  called Tensor.
It can be used in two ways: i) In python as a lybrary which is TensorFlow/tensor. ii)In html file as tensorflow.js where by you import a URL code for it which is 'src="https://cdn.jsdelivr.net/npm/@tensorflow/tfjs"'
For the above video, I have delt only with some arrays using the following operations, getting the data behind a tensor using tensor.data(),getting array behind the tensor using tensor.array(),getting the shape/size behind the tensor using tensor.shape().
